# Copy to .env for local `netlify dev`. In production, set this in Netlify env vars instead.
# Rotate this key before deploying — generate a fresh nilAI key for the hosted app.
NILAI_API_KEY=your-nilai-key-here
