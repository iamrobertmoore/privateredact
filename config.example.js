/* Copy this file to config.js and fill in your values. config.js is gitignored.
 *
 * Local / private use: put your nilAI key in `apiKey`.
 * Public deployment:  leave `apiKey` empty and set `proxyUrl` to a serverless
 *                     endpoint that holds the key, so it never reaches the browser.
 */
window.NILAI = {
  apiKey: '',
  baseUrl: 'https://api.nilai.nillion.network',
  model: 'google/gemma-4-26B-A4B-it',
  proxyUrl: ''
};
